# IE-Photo v2 — ยกเครื่อง UX/UI + ระบบใหม่

เอกสารนี้สรุปสิ่งที่เปลี่ยนใน v2 และขั้นตอน deploy ขึ้น https://iephoto.web.app

---

## 1. สรุปสิ่งที่เปลี่ยน

### 1.1 ธีมและ UX/UI

ยกเครื่องทั้งหมดให้ใช้ระบบดีไซน์ชุดเดียวกับหน้าเว็บหลัก (`index.html` / iephoto.online)
แทนธีม Dark Neon เดิมที่อยู่ในซอร์สแต่ยังไม่เคย deploy

| โทเคน | ค่า |
|---|---|
| สีหลัก (faculty) | `#ef3961` (ปรับได้จากหน้า `/settings`) |
| พื้นหลัง | `#f5f5f7` |
| สีหมึก | `#1d1d1f` |
| สีรอง | `#86868b` |
| ฟอนต์ | Inter + Kanit |
| easing | `cubic-bezier(0.16, 1, 0.3, 1)` |
| ผิวการ์ด | Apple Liquid Glass — `blur(36px) saturate(210%)` |

จุดที่ออกแบบเพื่อมือถือโดยเฉพาะ:

- **แถบนำทางล่าง** (`components/tabbar.tsx`) — 5 ปลายทางหลักตามบทบาท ซ่อนอัตโนมัติบนจอใหญ่
- **เมนูสไลด์** (React Bits `StaggeredMenu`) — แทนเมนู hamburger เดิม ล็อกสกรอลล์ ปิดด้วย Esc/แตะนอกเมนู
- **Modal เป็น bottom sheet** บนจอเล็ก มีที่จับลากแบบ iOS และเว้น safe-area ให้ iPhone
- ช่องกรอกทุกช่องขนาด `16px` — กัน iOS Safari ซูมเองตอนโฟกัส
- เป้ากดขั้นต่ำ 44px (`.tap`) ตามแนวทาง Apple HIG
- เอฟเฟกต์ hover ถูกจำกัดด้วย `@media (hover: hover)` — บนสัมผัสใช้ "กดแล้วยุบ" (`.press`) แทน
- เคารพ `prefers-reduced-motion` ทั้งระบบ

เอฟเฟกต์จาก React Bits ที่ติดตั้ง (`components/reactbits/`):

| คอมโพเนนต์ | ใช้ที่ไหน |
|---|---|
| `ClickSpark` | ทั้งแอป (canvas ใบเดียวใน layout) |
| `StaggeredMenu` | เมนูหลักบนมือถือ |
| `GlareHover` | การ์ดสตูดิโอ / ตากล้อง / ทางลัด / สถิติ |
| `AnimatedList` | ฟีดกิจกรรม |
| `Folder` | การ์ดงานส่งในหน้า `/deliveries` |
| `SpecularButton` | ปุ่ม CTA หลักหน้า `/book` |

> ทุกตัวถูกแก้จากต้นฉบับให้เข้ากับโปรเจกต์ — ดูคอมเมนต์หัวไฟล์ว่าแก้อะไรและเพราะอะไร

### 1.2 มุมมองประธาน / กรรมการ (v2.1)

**`/overview` — ภาพรวมชุมนุม** เรียงข้อมูลตามลำดับที่ผู้บริหารต้องการจริง อ่านจบใน 5 วินาที

1. **ต้องลงมือวันนี้** — รออนุมัติ / รอตรวจคืน / รออัปไฟล์ / เลยกำหนดส่ง แต่ละใบกดแล้วไปหน้าจัดการทันที
   (ไม่มีงานค้าง = แสดงการ์ดเขียวใบเดียว ไม่ต้องอ่านเลข 4 ตัวเปล่า ๆ)
2. **ชีพจรชุมนุม** — จำนวนงาน 30 วัน เทียบรอบก่อนเป็น % + กราฟแท่งรายวัน 14 วัน + สัดส่วนตามประเภท
3. **งานค้างอยู่ขั้นไหน** — สรุป workflow 4 ขั้น กดเข้าบอร์ดเต็มได้
4. **ภาระงานของทีม** — แถบเทียบว่าใครแบกเยอะ ใครว่าง (คิวถ่าย + งานส่ง + งานที่มอบหมาย)
5. คิวที่กำลังจะถึง + รายชื่อกรรมการชุดปัจจุบัน พร้อมปุ่มโทร

**`/workflow` — บอร์ดติดตามงาน** 5 ขั้น: คำขอใหม่ → ยืนยันแล้ว → กำลังดำเนินงาน → เก็บงาน → ปิดงาน
- มือถือ: คอลัมน์กว้าง 84% ของจอ ปัดทีละใบแบบ snap (คอลัมน์ถัดไปโผล่ขอบมาให้รู้ว่าปัดต่อได้)
- จอใหญ่: เห็นครบ 5 คอลัมน์พร้อมกัน
- อนุมัติ / ยืนยันรับคืน ได้จากในการ์ดเลย — ลดจำนวนคลิกเหลือ 1
- จัดขั้นจากสถานะ + เวลาจริง เช่น อนุมัติแล้วแต่เลยเวลาไปแล้ว จะเด้งไปขั้น "เก็บงาน" อัตโนมัติ

**ยศในชุมนุม แยกจากสิทธิ์ในระบบ**

| แกน | คืออะไร | ตั้งที่ไหน |
|---|---|---|
| **สิทธิ์ (role)** | เข้าหน้าไหนได้ / เขียนอะไรได้ — คุมด้วย `firestore.rules` | `/users` |
| **ยศ (title)** | ตำแหน่งในชุมนุม เช่น ประธานชุมนุม เลขานุการ — ข้อความล้วน ไม่มีผลต่อสิทธิ์ | `/users` (ตัวเลือกแก้ได้ที่ `/settings`) |

ประธานตั้งยศให้ตัวเองได้ (rules อนุญาตเฉพาะฟิลด์ `title` ของ doc ตัวเอง) ส่วนสิทธิ์ยังต้องให้คนอื่นตั้งให้เหมือนเดิม

**ทีมตากล้องเลือกจากสมาชิกชุมนุมได้**
- `/crew` → ปุ่ม **+ จากสมาชิก** เลือกหลายคนพร้อมกัน ระบบดึงชื่อ รูปโปรไฟล์ และยศมาให้อัตโนมัติ พร้อมผูก `uid` ให้เลย
- คนที่อยู่ในทีมแล้วไม่โผล่ซ้ำ · เรียงตามยศ (ประธาน → กรรมการ → สมาชิก)
- ในโหมดแก้ไขรายคน เลือกสมาชิกแล้วเติมช่องที่ยังว่างให้ ไม่ทับของที่พิมพ์ไว้เอง

### 1.3 ระบบใหม่

**จองตากล้อง** (`bookingType: "photographer"`)
- `/photographers` — สมาชิกเลือกและจอง
- `/crew` — แอดมินจัดการทีม (ชื่อ รูป ความถนัด เปิด/ปิดรับงาน ลำดับแสดง)
- `/book` — บุคคลภายนอกจองได้ (แท็บแยกจากสตูดิโอ)
- ฟิลด์เพิ่ม: `location`, `crewSize`, `usageType` (ประเภทงาน — ตัวเลือกตั้งได้ใน `/settings`)
- กันคิวชนด้วย `slots` ชุดเดียวกับอุปกรณ์/สตูดิโอ

**ตั้งค่าระบบจากหน้าเว็บ** (`/settings` → `settings/app`)
- ชื่อระบบ คำโปรย เบอร์/อีเมล ลิงก์อัลบั้ม แถบประกาศ **สีหลักของทั้งเว็บ**
- กติกาการจอง: จองล่วงหน้ากี่วัน ยืมนานสุดกี่วัน สตูดิโอกี่ชั่วโมง บังคับแนบเอกสารไหม เปิดให้คนนอกจองไหม
- ประเภทงานตากล้อง จำนวนตากล้องสูงสุด
- โดเมน NAS คำแนะนำการอัปไฟล์ กำหนดส่งงานเริ่มต้น
- เปิด/ปิดฟีเจอร์รายตัว — ปิดแล้วเมนูและหน้าที่เกี่ยวข้องหายจากทุกบัญชีทันที

ค่าทั้งหมดอ่านผ่าน `SettingsProvider` (`onSnapshot`) → แก้แล้วมีผลทุกเครื่องโดยไม่ต้องรีเฟรช

**สร้างฟอร์มเอง**
- `/form-builder` — แอดมินสร้าง/แก้ไข มีแท็บ คำถาม / ตัวอย่าง / ตั้งค่า
- ชนิดฟิลด์: ข้อความสั้น ข้อความยาว ตัวเลข อีเมล เบอร์โทร วันที่ เวลา วันที่+เวลา ตัวเลือก(select) เลือก 1 ข้อ เลือกหลายข้อ แนบรูป หัวข้อคั่น
- แชร์เป็นลิงก์สาธารณะ `/form/?id=<formId>` (ใช้ query param เพราะ static export ไม่มี dynamic route)
- เลือกได้ว่าให้คนนอกกรอกได้หรือต้องล็อกอิน / ผูกกับ flow จองอุปกรณ์-สตูดิโอ-ตากล้องได้
- `/responses` — ดูคำตอบ กรองตามฟอร์ม ดาวน์โหลด CSV (มี BOM ให้ Excel อ่านภาษาไทยได้)

**ส่งงาน / ลิงก์ NAS** (`/deliveries`)
- แอดมินสร้างงานส่ง ผูกกับการจองได้ (เติมชื่อลูกค้าอัตโนมัติ)
- เก็บลิงก์ 2 เส้น: `uploadUrl` (ให้ทีมงานอัปขึ้น NAS) และ `downloadUrl` (ให้ลูกค้ารับไฟล์) + รหัสผ่านลิงก์ + วันหมดอายุ
- สถานะ: รออัปไฟล์ → อัปแล้วรอตรวจ → ส่งงานแล้ว → ปิดงาน
- ทีมงานที่รับผิดชอบกดปุ่ม "อัปไฟล์เสร็จแล้ว" ได้เอง (rules อนุญาตแก้แค่ `status` เท่านั้น แตะลิงก์ไม่ได้)
- ระบบเตือนถ้าลิงก์ที่วางไม่ได้อยู่บนโดเมน NAS ที่ตั้งไว้

### 1.4 หน้าเดิมที่ปรับ

ทุกหน้าเปลี่ยนมาใช้ระบบดีไซน์ใหม่ และเพิ่ม:

- **แดชบอร์ด** — การ์ดที่ต้องจัดการเน้นสีเมื่อมีค้าง + รายการรออนุมัติ + คิวที่กำลังจะถึง
- **รายการจอง** — ช่องค้นหา ชิปกรอง ปุ่มโทร/อีเมลกดได้ กล่องรายละเอียด
- **ยืมอุปกรณ์** — กรองตามประเภท เลือกหลายชิ้น ตัวเลือกแนบรูปพร้อมพรีวิว จำกัดวันตาม settings
- **การจองของฉัน** — ชิปกรองสถานะ นับถอยหลัง "เริ่มอีก N วัน"
- **ปฏิทิน** — จัดกลุ่มตามวัน หัววันเป็น sticky กรองตามประเภท
- **คลังอุปกรณ์** — ค้นหา กรอง แก้ไขชื่อ/ประเภท/หมายเหตุได้
- **สมาชิก** — ค้นหา กรองตามบทบาท/ถูกระงับ
- **ฟีด** — ทางลัด 4 ปุ่มใหญ่ + สรุปการจองของตัวเอง + ฟีดกิจกรรม

### 1.5 คุณภาพโค้ด

- `npm run build` ผ่าน (27 routes, static export)
- `npx eslint .` — 0 error (React Compiler rules ครบ)
- `npm test` — เทสตรรกะสรุปตัวเลขใน `lib/analytics.ts` 25 เคส ผ่านหมด
  (รันด้วย `node --experimental-strip-types` ไม่ต้องลง test runner เพิ่ม)
- เพิ่ม `useNow()` ใน `lib/hooks.ts` — อ่านเวลาปัจจุบันแบบใช้ตอน render ได้ผ่าน `useSyncExternalStore`
  (แทนการเรียก `Date.now()` ตรง ๆ ซึ่งเป็น impure) และทำให้ป้าย "อีก 3 นาที" อัปเดตเองทุกนาที
- เลิกใช้ `useEffect` sync state หลายจุด เปลี่ยนเป็น derived state — ลด render ซ้อนและกันค่าที่กำลังพิมพ์โดนเขียนทับ

---

## 2. โครงสร้างข้อมูลใหม่

| Collection | ใครอ่านได้ | ใครเขียนได้ |
|---|---|---|
| `settings/app` | ทุกคน (หน้า `/book`, `/form` ต้องใช้) | แอดมิน |
| `photographers` | ทุกคน — **ห้ามเก็บเบอร์/อีเมลส่วนตัว** | แอดมิน |
| `forms` | ทุกคน (ต้องอ่าน schema ได้ก่อนล็อกอิน) | แอดมิน + ผู้ส่งฟอร์มบวก `responseCount` ได้ 1 |
| `formResponses` | แอดมิน + เจ้าของคำตอบ | สร้างได้ถ้าฟอร์มเปิดอยู่ (คนนอกได้เฉพาะฟอร์มที่อนุญาต) |
| `deliveries` | แอดมิน + ลูกค้าเจ้าของงาน + ทีมงานที่รับผิดชอบ | แอดมิน (ทีมงานแก้ได้แค่ `status` จาก `awaiting_upload` → `uploaded`) |

`bookings` และ `slots` รับ `bookingType: "photographer"` เพิ่ม และ `bookings` รับฟิลด์ `location` / `crewSize` (validate ใน rules แล้ว)

---

## 3. ขั้นตอน deploy

> ต้องมี `firebase-tools` และล็อกอินบัญชีที่เข้าถึงโปรเจกต์ `iephoto` ได้

### 3.1 เตรียม env

```bash
cd "iephoto_management/ie-photo-next"
cp .env.local.example .env.local   # ถ้ายังไม่มี
```

ค่า `NEXT_PUBLIC_*` ในไฟล์ตัวอย่างเป็นค่าจริงของโปรเจกต์อยู่แล้ว
ส่วน `FIREBASE_CLIENT_EMAIL` / `FIREBASE_PRIVATE_KEY` ใส่เฉพาะเวลาจะรัน `scripts/*.cjs`

### 3.2 Deploy rules + indexes (ต้องทำก่อน ไม่งั้นฟีเจอร์ใหม่พัง)

```bash
npx firebase deploy --only firestore:default
```

> ⚠️ ต้องใช้ `firestore:default` (ชื่อ database) **ไม่ใช่** `firestore:rules`
> เพราะ `firebase.json` ตั้ง `firestore` เป็น array แบบ named database — สั่ง `firestore:rules`
> จะขึ้น "Deploy complete" แต่ไม่ได้อัปอะไรเลย ส่วน `firestore:rules,firestore:indexes`
> จะพังด้วย `TypeError: Cannot read properties of undefined (reading 'map')`

> ถ้าไม่ deploy rules ก่อน หน้า `/photographers`, `/forms`, `/deliveries`, `/settings`
> จะขึ้น `Missing or insufficient permissions`

### 3.3 Seed ค่าตั้งต้น (ครั้งเดียว)

```bash
node --env-file=.env.local scripts/seed-v2.cjs
```

สร้าง `settings/app`, ทีมตากล้องตั้งต้น 1 รายการ และฟอร์มตัวอย่าง 1 ใบ
รันซ้ำได้ — `settings` ใช้ merge ส่วน `photographers`/`forms` ข้ามถ้ามีข้อมูลอยู่แล้ว

### 3.4 Build + deploy เว็บ

```bash
npm run build && npx firebase deploy --only hosting
```

### 3.5 ตรวจหลัง deploy

1. เปิด https://iephoto.web.app/book/ — ต้องเห็นแท็บสตูดิโอและตากล้อง
2. ล็อกอินแอดมิน เปิด `/settings` ลองเปลี่ยนสีหลัก แล้วดูว่าเว็บเปลี่ยนสีทันที
3. `/crew` เพิ่มตากล้อง แล้วเช็คว่าโผล่ในหน้า `/book`
4. `/form-builder` สร้างฟอร์ม เปิดใช้งาน ก็อปลิงก์ไปเปิดในโหมดไม่ล็อกอิน
5. `/deliveries` สร้างงานส่ง วางลิงก์ NAS แล้วเช็คจากบัญชีทีมงาน

### 3.6 ถ้าต้องย้อนกลับ

Firebase Hosting เก็บ version เดิมไว้ — ย้อนได้จาก Console → Hosting → เลือก release เดิม → Rollback
ส่วน rules ย้อนได้จาก Console → Firestore → Rules → History

---

## 4. เรื่องที่ยังต้องตัดสินใจ / ทำต่อได้

- **ลิงก์ NAS ยังต้องวางเอง** — ถ้าอยากให้ระบบสร้างโฟลเดอร์ + share link อัตโนมัติ
  ต้องมี backend (Cloud Functions = ต้องอัปเป็น Blaze plan) เพื่อเก็บ app password ของ Nextcloud ฝั่งเซิร์ฟเวอร์
  โครงสร้างข้อมูล `deliveries` ออกแบบเผื่อไว้แล้ว — เพิ่มแค่ตัวสร้างลิงก์
- **แจ้งเตือนงานส่ง** — `scripts/send-notifications.cjs` ยังเตือนเฉพาะ tasks/bookings ยังไม่รวม `deliveries`
- **ฟอร์มยังไม่มี conditional logic** (ถ้าตอบ A ให้โชว์ข้อ B) และยังไม่แบ่งหลายหน้า
